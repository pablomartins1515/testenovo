import { MouseEvent } from 'react';

export function ScrollToTop(event: MouseEvent<HTMLButtonElement>) {
  window.scrollTo({
    top: 0,
    behavior: 'smooth'
  });

      return (
      
        <div >          
        </div >
    )
}

